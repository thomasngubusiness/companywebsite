// Optional: only needed if the backend is on a DIFFERENT origin than the site.
// Include before forms.js:  <script src="assets/js/config.example.js"></script>
window.SITE_CONFIG = { apiBase: 'https://api.example.com/api' };
